[Unit]
Description=Thanos Store Gateway Deployment
Before=local-fs.target

[Kube]
Yaml=/opt/deployment/thanos_storegw.yaml

[Install]
# Start by default on boot
WantedBy=multi-user.target default.target